{
  "_comment": "Watches posts mentioning the authorized X user. user_id defaults to that user.",
  "schema_version": 1,
  "required": [["credential_id"]],
  "identity": ["credential_id", "user_id", "filter_keywords", "exclude_user_ids"],
  "checker_example": {"type": "x_mentions", "credential_id": "default", "last_checked_ts": "1"},
  "open_loop": true,
  "user_creatable": true,
  "upstream": "x"
}
