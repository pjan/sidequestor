.venv/
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]
.DS_Store

# Local design drafts and generated reaction artwork.
docs/explainer/
src/sidequestor/runtime/yaas-triage/assets/reactions/

# Local operator skills and disposable end-to-end workspaces.
.agents/
e2e/
.env

# slack_credentials.py compiles its Keychain helper relative to the package root
# (surfaces/slack_credentials.py:59), so running anything that touches Slack creds from
# a source checkout drops a binary here. It belongs in a workspace, not in the repo.
src/sidequestor/runtime/state/
